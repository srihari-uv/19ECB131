#include <stdio.h>

int main() {
    int i=10;
    int *ptr=&i;
    printf("value of i= %d, address of i is %u",i,&i);
    printf("\n value of  pointer is %u,value pointed by the ptr is %d",ptr,*ptr);
    *ptr=60;
    printf("\n %d",i);

    return 0;
}
//u is an unsigned int format specifier ig.
