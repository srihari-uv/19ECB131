
int main() {
    char fileName[100];
    printf("Enter the file name: ");
    scanf("%s", fileName);
    getchar(); // Consume newline character left by scanf

    searchStudent(fileName);

    return 0;
}
